name: CI/CD con SonarCloud y Deploy a GitHub Pages

on:
  push:
    branches:
      - master  # Cambios en master disparan el flujo

jobs:
  build_and_analyze:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del repo
      uses: actions/checkout@v3

    # Paso para verificar que los secretos están definidos correctamente
    - name: Verificar secretos
      run: |
        echo "✅ Verificando los secretos..."
        if [ -z "${{ secrets.SONAR_PROJECT_KEY }}" ]; then
          echo "🚨 SONAR_PROJECT_KEY no está definido"
        else
          echo "✅ SONAR_PROJECT_KEY está definido"
        fi

        if [ -z "${{ secrets.SONAR_ORGANIZATION }}" ]; then
          echo "🚨 SONAR_ORGANIZATION no está definido"
        else
          echo "✅ SONAR_ORGANIZATION está definido"
        fi

        # Importante: No imprimas SONAR_TOKEN o GITHUB_TOKEN por seguridad
        echo "✅ Verificación completada"

    # Ejecutar análisis de SonarCloud
    - name: SonarCloud Scan
      uses: sonarsource/sonarcloud-github-action@v2.3
      with:
        SONAR_PROJECT_KEY: ${{ secrets.SONAR_PROJECT_KEY }}  # Usando el nombre correcto para el secreto
        SONAR_ORGANIZATION: ${{ secrets.SONAR_ORGANIZATION }}  # Usando el nombre correcto para el secreto
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Token de acceso de GitHub
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}  # Token de acceso de SonarCloud
        SONAR_HOST_URL: https://sonarcloud.io  # URL de SonarCloud

  deploy:
    needs: build_and_analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repo
        uses: actions/checkout@v3

      # Otros pasos de despliegue aquí...

